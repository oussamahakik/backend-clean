================================================================================
  COMMANDES GITHUB - BACKEND ET FRONTEND
================================================================================

⚠️  IMPORTANT : Remplacez "VOTRE_USERNAME" par votre nom d'utilisateur GitHub
⚠️  Créer les dépôts sur GitHub AVANT d'exécuter ces commandes
    (sans initialiser avec README.md)

================================================================================
📦 BACKEND
================================================================================

cd "C:\Users\pc\OneDrive - UMONS\Bureau\caisse\caisse"

git init
git add .
git commit -m "Initial commit: Backend Spring Boot - Système de gestion de caisse"
git branch -M main
git remote add origin https://github.com/VOTRE_USERNAME/caisse-backend.git
git push -u origin main

================================================================================
🎨 FRONTEND
================================================================================

cd "C:\Users\pc\OneDrive - UMONS\Bureau\caisse_manager\caisse-manager-ui"

git add .
git commit -m "Initial commit: Frontend React - Interface de gestion de caisse"
git branch -M main
git remote set-url origin https://github.com/VOTRE_USERNAME/caisse-manager-ui.git
git push -u origin main

================================================================================
🔐 AUTHENTIFICATION
================================================================================

Si GitHub demande un mot de passe :
1. Aller sur GitHub → Settings → Developer settings → Personal access tokens
2. Générer un nouveau token (classic) avec permission "repo"
3. Utiliser ce token comme mot de passe lors du push

================================================================================
✅ VÉRIFICATION
================================================================================

Après le push, vérifier que :
- Les dépôts sont créés sur GitHub
- Les fichiers sont présents
- Le fichier application.properties n'est PAS dans le repo backend (sécurité)
- Le fichier application.properties.example est présent

================================================================================

